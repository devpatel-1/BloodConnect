<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="6dp"
    android:padding="16dp"
    android:background="#FFFFFF">

    <!-- Donor Name -->
    <TextView
        android:id="@+id/requestDonorName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="17sp"
        android:textStyle="bold"
        android:textColor="#212121"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="Patel Dev Amitkumar" />

    <!-- Blood Group -->
    <TextView
        android:id="@+id/requestBloodGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textColor="#666666"
        android:layout_marginTop="6dp"
        app:layout_constraintTop_toBottomOf="@id/requestDonorName"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="Blood Group: AB-" />

    <!-- Status -->
    <TextView
        android:id="@+id/requestStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="15sp"
        android:textStyle="bold"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/requestBloodGroup"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="● Request Pending" />

    <!-- Status Explanation -->
    <TextView
        android:id="@+id/requestStatusMessage"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textColor="#666666"
        android:layout_marginTop="4dp"
        app:layout_constraintTop_toBottomOf="@id/requestStatus"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="Waiting for the donor to respond." />

    <!-- Request Date -->
    <TextView
        android:id="@+id/requestDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textColor="#777777"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/requestStatusMessage"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="Requested: 16 Sep 2026, 04:25 PM" />

</androidx.constraintlayout.widget.ConstraintLayout>